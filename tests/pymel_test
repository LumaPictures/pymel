#!/usr/bin/env mayapy

#nosetests --with-doctest -v pymel --exclude '(windows)|(tools)|(arrays)|(example1)'

#import doctest
import sys, os.path
thisDir = os.path.dirname(os.path.join(sys.path[0], sys.argv[0]))
execfile(os.path.join(thisDir, 'pymel_test.py'))
